#@TYPE: Machine
#@NAME: Nuvoton NuMaker IoT MA35D0 Evaluation Board
#@DESCRIPTION: Machine configuration for Nuvoton NuMaker-IoT-MA35D0 (512MB DDR, SPI NAND)

require conf/machine/include/ma35d0.inc

KERNEL_DEVICETREE = "nuvoton/ma35d0-iot-512m.dtb"
KERNEL_IMAGETYPES = "Image"

# U-Boot configuration for SPI NAND boot (primary for BMC use case)
UBOOT_MACHINE = "ma35d0_spinand_defconfig"

# TF-A settings
TFA_PLATFORM = "ma35d0"
TFA_DTB = "ma35d0-cpu650-custom-ddr"
TFA_DDR_HEADER = "MA35D03F867C_MCP_WBDDR3L_256M_1PCS.h"
TFA_DDR_SIZE = "0x10000000"
TFA_SRCREV = "master"
TFA_PMIC = "0"
TFA_LOAD_CORE1 = "no"
TFA_LOAD_SCP = "no"

# OpenBMC features - suitable for a 512MB RAM system
IMAGE_FEATURES += "ssh-server-openssh"

OBMC_IMAGE_EXTRA_INSTALL:append = " packagegroup-ma35d0-apps"

PREFERRED_PROVIDER_virtual/obmc-chassis-mgmt = "packagegroup-ma35d0-apps"
PREFERRED_PROVIDER_virtual/obmc-flash-mgmt = "packagegroup-ma35d0-apps"
PREFERRED_PROVIDER_virtual/obmc-system-mgmt = "packagegroup-ma35d0-apps"

# Disable features not needed on this BMC platform
IMAGE_FEATURES:remove = " \
    obmc-host-ipmi \
    obmc-net-ipmi \
    obmc-ikvm \
    obmc-user-mgmt-ldap \
    obmc-telemetry \
"

IMAGE_INSTALL:remove = "rest-dbus"

DISTRO_FEATURES:append = " phosphor-no-ipmi-rmcp"
DISTROOVERRIDES:append = ":df-phosphor-no-ipmi-rmcp"

# Use systemd as init manager (standard for OpenBMC)
VIRTUAL-RUNTIME_init_manager = "systemd"
VIRTUAL-RUNTIME_initscripts = ""

# Kernel image is raw Image for aarch64
FLASH_KERNEL_IMAGE = "Image-${MACHINE}.bin"
